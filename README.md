# Algorithmic_Trading_API
Repositório destinado a armazenar API back-end simples focada em mercado financeiro (Fin Tech)

# Instalar libs: 

pip install "fastapi==0.104.*" "pydantic==2.*" backtrader yfinance pandas numpy "SQLAlchemy==2.*" alembic "uvicorn[standard]" pytest python-dotenv